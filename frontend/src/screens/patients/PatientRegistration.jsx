// TODO(Aviel): build the registration form per the Figma design.
// Submit handler is already wired to the real backend endpoint —
// registerPatient({ name, age, contact, ailment }) from
// src/api/patients.js — you just need the form markup and state.
export default function PatientRegistration() {
  return (
    <section>
      <h1>Register Patient</h1>
      <p>TODO: form (name, age, contact, ailment) -&gt; registerPatient()</p>
    </section>
  );
}
