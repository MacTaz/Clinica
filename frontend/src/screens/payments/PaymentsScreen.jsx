// TODO(Agatha, with Mico on the appointment-picker part): pick an
// appointment, enter amount + method (CASH/CARD/GCASH), submit via
// recordPayment(appointmentId, {amount, method}); below that, a table of
// all payments via getPayments(). Both calls in src/api/payments.js are
// already wired to the backend.
export default function PaymentsScreen() {
  return (
    <section>
      <h1>Payments</h1>
      <p>TODO: record-payment form + payments table -&gt; recordPayment() / getPayments()</p>
    </section>
  );
}
