// TODO(Aviel): list doctors with their specialization + weekly schedule.
// getDoctors() from src/api/doctors.js is already wired to the backend.
export default function DoctorDirectory() {
  return (
    <section>
      <h1>Doctors</h1>
      <p>TODO: table of doctors -&gt; getDoctors()</p>
    </section>
  );
}
