// TODO(Aviel): registration form per the Figma design, including the
// weekly schedule builder (day + start/end time, add/remove rows).
// registerDoctor({ name, age, contact, specializationId, salary, schedules })
// from src/api/doctors.js is already wired to the backend.
export default function DoctorRegistration() {
  return (
    <section>
      <h1>Register Doctor</h1>
      <p>TODO: form -&gt; registerDoctor()</p>
    </section>
  );
}
