// TODO(Mico): specialization selector -> react-day-picker calendar ->
// getAvailability(specializationId, date) -> list of doctors/free slots ->
// select doctor + slot -> bookAppointment({patientId, doctorId,
// appointmentDate, startTime}). All three api/*.js calls already exist
// and are wired to the backend (mock data for now).
export default function BookAppointment() {
  return (
    <section>
      <h1>Book Appointment</h1>
      <p>TODO: specialization + calendar + slot picker -&gt; bookAppointment()</p>
    </section>
  );
}
