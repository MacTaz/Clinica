// TODO(Mico): list appointments, with a cancel button per row.
// getAppointments() / cancelAppointment(id) from src/api/appointments.js
// are already wired to the backend.
export default function AppointmentList() {
  return (
    <section>
      <h1>Appointments</h1>
      <p>TODO: table of appointments -&gt; getAppointments() / cancelAppointment()</p>
    </section>
  );
}
